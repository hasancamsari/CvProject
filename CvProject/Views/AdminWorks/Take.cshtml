@model CvProject.Models.Entity.TBLPROJECTS
@{
    ViewBag.Title = "Take";
    Layout = "~/Views/AdminDefault/_AdminMain.cshtml";
}

@using (Html.BeginForm("UpdateWorks", "AdminWorks", FormMethod.Post))
{
    @Html.HiddenFor(m => m.PID)

    <div class="container mt-4">
        <div class="card shadow rounded">
            <div class="card-body">
                <h5 class="card-title mb-4">Bilgileri Giriniz</h5>

                <div class="mb-3">
                    <label class="form-label">Deneyim Giriniz</label>
                    @Html.TextBoxFor(m => m.JOBS, new { @class = "form-control" })
                </div>
                <div class="mb-3">
                    <label class="form-label">Proje Giriniz</label>
                    @Html.TextBoxFor(m => m.WORKS, new { @class = "form-control" })
                </div>

                <button type="submit" class="btn btn-primary">Gönder</button>
            </div>
        </div>
    </div>
}
