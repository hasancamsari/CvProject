@model CvProject.Models.Entity.TBLSKILLS
@{
    ViewBag.Title = "Take";
    Layout = "~/Views/AdminDefault/_AdminMain.cshtml";
}

@using (Html.BeginForm("UpdateSkills", "AdminSkills", FormMethod.Post))
{
    @Html.HiddenFor(m => m.SID)

    <div class="container mt-4">
        <div class="card shadow rounded">
            <div class="card-body">
                <h5 class="card-title mb-4">Bilgileri Giriniz</h5>

                <div class="mb-3">
                    <label class="form-label">İsim Giriniz</label>
                    @Html.TextBoxFor(m => m.SKILLS, new { @class = "form-control" })
                </div>

                <button type="submit" class="btn btn-primary">Gönder</button>
            </div>
        </div>
    </div>
}
